{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch",
            "url": "http://localhost:1313",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "start server",
            "postDebugTask": "stop server",
        }
    ]
}